spring:
  task:
    scheduling:
      enabled: false
  datasource:
    url: jdbc:tc:postgresql:16.8:///link_platform_test_${random.uuid}
    username: link_platform
    password: link_platform
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  kafka:
    listener:
      auto-startup: false

link-platform:
  analytics:
    outbox-relay-delay: 600000
  lifecycle:
    outbox-relay-delay: 600000
  projection-jobs:
    runner-delay: 600000
  webhooks:
    # Tests stay strict by default.
    # Individual integration tests opt in explicitly when they need HTTP and/or private callback allowances.
    allow-private-callback-hosts: false
    allow-http-callbacks: false
    runner-delay-ms: 600000
  exports:
    runner-delay-ms: 600000
