# Operational Signals

Signals: redirect latency, cache ratio, Redis fallback, worker lag, freshness, parked events, poison count, projection status, quota rejections, webhook failures, query fallback, replica lag, failover events.

Back to [README](../../README.md).
