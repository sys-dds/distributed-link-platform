# Local Development

Local development uses Docker Compose plus the Java API. The stack models PostgreSQL, Kafka, Redis, control-plane, redirect runtimes, worker runtime, query fallback proof, and no-failover proof.

Back to [README](../../README.md).
