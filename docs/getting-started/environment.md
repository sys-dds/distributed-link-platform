# Environment

Important config includes DB_URL, QUERY_DB_URL, Kafka bootstrap servers, Redis host/port, runtime mode, redirect region, failover region/base URL, public base URL, lifecycle topic, cache flag, quota settings, and webhook settings.

Back to [README](../../README.md).
