# Async Analytics Pipeline

The async analytics pipeline includes Kafka, transactional outbox, ordered processing, worker runtime, relay safety, retries, dead-letter parking, leases, lease recovery, backpressure, and poison-message handling.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
