# Replica Aware Reads

Replica-aware reads include query datasource routing, health indicators, fallback to primary, runtime state, fallback records, lag policy, and role-aware metrics.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
