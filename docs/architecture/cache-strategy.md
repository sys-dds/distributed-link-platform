# Cache Strategy

Redis accelerates hot reads, but PostgreSQL remains source truth. Cache failure should degrade explicitly and cache hit/miss behavior should be measurable.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
