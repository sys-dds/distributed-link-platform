# Public Redirect Path

The public redirect path handles slug lookup, cache lookup, durable fallback, redirect response, click event emission, regional failover, and degraded behavior.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
