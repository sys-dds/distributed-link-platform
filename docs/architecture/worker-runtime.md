# Worker Runtime

The worker runtime handles async analytics processing, outbox relay/consumption, projection updates, retries, parking, leases, and backpressure.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
