# Runtime Roles

Runtime roles make traffic shape explicit without premature microservices: control plane for owners/operators, redirect for public hot path, worker for async analytics, proof runtimes for degradation/failover.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
