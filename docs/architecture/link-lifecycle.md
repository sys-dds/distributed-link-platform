# Link Lifecycle

Link lifecycle includes create, read, list, lifecycle mutation, expiration, lifecycle-aware reads, search, filtering, metadata, and discovery.

## Why it matters

This area turns DLP from a simple short-link service into a production-shaped backend platform.

Back to [README](../../README.md).
